{%- if "postgress" in key -%}
    {% set connector_name = "postgresql" %}
{%- elif "mysql" in key -%}
    {% set connector_name = "mysql" %}
{%- endif -%}
connector.name={{connector_name}}
connection-url={{url}}
connection-user={{user}}
connection-password={{password}}